local status_ok, flash = pcall(require, "flash")
if not status_ok then
  return
end

-- require('flash').setup ({
--   modes = {
--     char = {
--       jump_labels = true --add labls for f, F, t, T
--     }
--   },
-- })
